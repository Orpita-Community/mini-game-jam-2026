namespace Orpaits.Core
{
    /// <summary>High-level game states tracked by <see cref="GameManager"/>.</summary>
    public enum GameState
    {
        MainMenu,
        Playing,
        Paused,
        Victory,
        Defeat
    }
}
